<!--pages/search/search.wxml-->
<view class="top">
  <searchBar></searchBar>
</view>
<view>
  <item wx:for="{{questionTitleData}}" question="{{item.title}}" aid="{{item.aid}}" wx:key="{{index}}"></item>
</view>
<view style='{{loading}}' class='loading'>
  <loading></loading>
</view>